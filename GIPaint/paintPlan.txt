#PSEUDOCODE  -  PAINT PROJECT PLAN

#Pseudocode - looks like code, but it's not (english-like code)

<setup> (resolution,colours,variables, load all pictures....)
     (use at least 1200*900)

while running:
    <event loop> 
  (inside the event loop you will check for mouse clicks, key presses....)

    #selecting the tool
    if <click on pencil tool>:
       tool="pencil"
    if <click on eraser tool>:
       tool="eraser"
    .....

    #use the tool
    if <click on canvas>:
       if tool=="pencil":
          draw.line(screen,col,(oldmx,oldmy),(mx,my))     
       if tool=="eraser":
          draw.circle(screen,bgcol,(mx,my),radius)


    #select (change) colour
    if <click on colour palette>:
        col=screen.get_at((mx,my))

    oldmx,oldmy=mx,my
    display.flip()
quit()


















